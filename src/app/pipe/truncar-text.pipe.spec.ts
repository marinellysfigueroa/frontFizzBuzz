import { TruncarTextPipe } from './truncar-text.pipe';

describe('TruncarTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncarTextPipe();
    expect(pipe).toBeTruthy();
  });
});
